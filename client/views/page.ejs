<!doctype html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport"
    content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/js/page.js" defer></script>
  <!-- include the Bootstrap CSS file -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">

  <!-- include the jQuery library (required for Summernote) -->
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>

  <!-- include the Bootstrap JavaScript bundle (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.bundle.min.js"></script>

  <!-- include the Summernote CSS file -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-bs4.min.css">

  <!-- include the Summernote JavaScript file -->
  <script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-bs4.min.js"></script>

</head>


<body>
  <div class="container mt-4">

    <div class="card">
      <div class="card-body py-3">
        <h5 class="card-title m-0">
          <span class="badge badge-secondary"><a title="Find other content with the 'question' tag">QUESTION</a></span>
          <span>What is the?</span>
        </h5>
      </div>
      <hr class="my-1">
      <div class="card-body py-3">
        <p class="card-text m-0"><small class="text-muted">Author: Alex Johnson</small></p>
        <p class="card-text m-0"><small class="text-muted">Date: January 30, 2020</small></p>
      </div>
    </div>




    <div class="container mt-4 position-relative">
      <div class="row border p-3">
        <div class="col-sm-3 align-self-center mt-2 text-center">
          <h5 class="mb-5 mt-2">Username</h5>
          <!-- <img src="loced.png" alt="Avatar" class="img-fluid rounded" style="max-width: 75px;"> -->
        </div>

        <div class="col-sm-9 align-self-start mt-2">
          <h5 class="text-muted mb-0">Date</h5>
          <p class="mt-4">This will set the maximum width of the image to 150 pixels,
            so that it will not become larger than this on a screen with a width of 575 pixels.

            Alternatively, if you want the image to maintain its original size regardless of the screen size,
            you can use the img-thumbnail class provided by Bootstrap instead of the img-fluid class.
            This will apply a border and background to the image, as well as add some padding,
            to give it the appearance of a thumbnail.
            However, the image will not scale up or down with the screen size.</p>
        </div>
      </div>
    </div>





    <div class="container mt-4">
      <div class="row border p-3 admin-block">
        <div class="col-sm-3 align-self-center mt-2 text-center">
          <h5 class="mb-5 mt-2">Username</h5>
          <!-- <img src="loced.png" alt="Avatar" class="img-fluid rounded" style="max-width: 75px;"> -->
        </div>
        <div class="col-sm-9 align-self-start mt-2">
          <h5 class="text-muted mb-0">Date</h5>
          <p class="mt-4">This will set the maximum width of the image to 150 pixels,
            so that it will not become larger than this on a screen with a width of 575 pixels.

            Alternatively, if you want the image to maintain its original size regardless of the screen size,
            you can use the img-thumbnail class provided by Bootstrap instead of the img-fluid class.
            This will apply a border and background to the image, as well as add some padding,
            to give it the appearance of a thumbnail.
            However, the image will not scale up or down with the screen size.</p>
        </div>
      </div>
    </div>

    <div class="container mt-4">
      <div class="row border p-3 user-block">
        <div class="col-sm-3 align-self-center mt-2 text-center">
          <h5 class="mb-5 mt-2">Username</h5>
          <!-- <img src="loced.png" alt="Avatar" class="img-fluid rounded" style="max-width: 75px;"> -->
        </div>
        <div class="col-sm-9 align-self-start mt-2">
          <h5 class="text-muted mb-0">Date</h5>
          <p class="mt-4">This will set the maximum width of the image to 150 pixels,
            so that it will not become larger than this on a screen with a width of 575 pixels.

            Alternatively, if you want the image to maintain its original size regardless of the screen size,
            you can use the img-thumbnail class provided by Bootstrap instead of the img-fluid class.
            This will apply a border and background to the image, as well as add some padding,
            to give it the appearance of a thumbnail.
            However, the image will not scale up or down with the screen size.</p>
        </div>
      </div>
    </div>


    <div class="container mt-4">
      <div class="row border p-3 admin-block">
        <div class="col-sm-3 align-self-center mt-2 text-center">
          <h5 class="mb-5 mt-2">Username</h5>
        </div>
        <div class="col-sm-9 align-self-start mt-2">
          <h5 class="text-muted mb-0">Date</h5>
          <p class="mt-4">This will set the maximum width of the image to 150 pixels,
            so that it will not become larger than this on a screen with a width of 575 pixels.

            Alternatively, if you want the image to maintain its original size regardless of the screen size,
            you can use the img-thumbnail class provided by Bootstrap instead of the img-fluid class.
            This will apply a border and background to the image, as well as add some padding,
            to give it the appearance of a thumbnail.
            However, the image will not scale up or down with the screen size.</p>
        </div>
      </div>
    </div>




    <div class="container mt-4" id="Containesubmit">
      <!-- Submit a Ticket Modal -->
      <form>
        <div class="form-group">
          <div id="summernote"></div>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
        <button type="button" class="btn btn-primary float-right" data-toggle="modal"
          data-target="#closeModal">Close</button>
      </form>
    </div>

    <script>
      $(document).ready(function () {
        $('#summernote').summernote({
          placeholder: 'Hello',
          tabsize: 2,
          height: 300,
          width: '100%',
        });

        // Confirm close action
        $('#confirmClose').click(function () {
          // Your code to close the topic goes here
          console.log('Topic closed');

          // Close the modal
          $('#closeModal').modal('hide');
        });
      });
    </script>

  </div>



  </div>


  <!-- Close Modal -->
  <div class="modal fade" id="closeModal" tabindex="-1" role="dialog" aria-labelledby="closeModalLabel"
    aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="closeModalLabel">Confirm Close</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          Are you sure you want to close this topic?
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
          <button type="button" class="btn btn-primary" id="confirmClose">Close</button>
        </div>
      </div>
    </div>
  </div>








</body>

</html>